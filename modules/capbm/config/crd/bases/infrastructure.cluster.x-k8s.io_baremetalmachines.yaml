---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: baremetalmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: BareMetalMachine
    listKind: BareMetalMachineList
    plural: baremetalmachines
    singular: baremetalmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Machine is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Provider ID
      jsonPath: .status.providerID
      name: ProviderID
      type: string
    - description: IP Address
      jsonPath: .spec.ipAddress
      name: IPAddress
      type: string
    - description: Time duration since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: BareMetalMachine is the Schema for the baremetalmachines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BareMetalMachineSpec defines the desired state of BareMetalMachine.
            properties:
              componentInstall:
                description: ComponentInstall holds configuration for automatic component
                  installation.
                properties:
                  airGap:
                    description: AirGap defines configuration for offline/air-gapped
                      installations.
                    properties:
                      binarySource:
                        default: HTTPServer
                        description: |-
                          BinarySource specifies how binaries are delivered in air-gapped mode.
                          Options: HTTPServer | ConfigMap | LocalPath
                        type: string
                      enabled:
                        description: Enabled indicates whether air-gapped installation
                          mode is used.
                        type: boolean
                      httpServerConfig:
                        description: HTTPServerConfig is the configuration for HTTP
                          binary source.
                        properties:
                          basePath:
                            description: BasePath is the base path for serving content.
                            type: string
                          baseUrl:
                            description: BaseURL is the HTTP server URL serving binary
                              packages.
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables the HTTP server.
                            type: boolean
                          insecureSkipVerify:
                            description: InsecureSkipVerify skips TLS verification
                              (for internal CAs).
                            type: boolean
                          port:
                            default: 8080
                            description: Port is the HTTP server port.
                            type: integer
                          tlsSecretRef:
                            description: TLSSecretRef references a Secret containing
                              TLS client certificate.
                            type: string
                        type: object
                      localPath:
                        description: LocalPath is the path on target machine where
                          binaries are pre-placed.
                        type: string
                      preloadImages:
                        description: PreloadImages is a list of container images to
                          preload into containerd.
                        items:
                          type: string
                        type: array
                    type: object
                  cni:
                    description: CNI defines the CNI plugin installation configuration.
                    properties:
                      airGap:
                        description: AirGap defines air-gapped installation configuration
                          for CNI.
                        properties:
                          chartArchive:
                            description: ChartArchive is the path to the Helm chart
                              archive (for Helm mode).
                            type: string
                          cniPluginsArchive:
                            description: CNIPluginsArchive is the path to the CNI
                              plugins binary archive.
                            type: string
                          enabled:
                            description: Enabled indicates whether air-gapped installation
                              is used.
                            type: boolean
                          httpServerConfig:
                            description: HTTPServerConfig is the HTTP server configuration.
                            properties:
                              basePath:
                                description: BasePath is the base path for serving
                                  content.
                                type: string
                              baseUrl:
                                description: BaseURL is the HTTP server URL serving
                                  binary packages.
                                type: string
                              enabled:
                                default: false
                                description: Enabled enables the HTTP server.
                                type: boolean
                              insecureSkipVerify:
                                description: InsecureSkipVerify skips TLS verification
                                  (for internal CAs).
                                type: boolean
                              port:
                                default: 8080
                                description: Port is the HTTP server port.
                                type: integer
                              tlsSecretRef:
                                description: TLSSecretRef references a Secret containing
                                  TLS client certificate.
                                type: string
                            type: object
                          localPath:
                            description: LocalPath is the local path for manifest
                              files.
                            type: string
                          manifestSource:
                            default: HTTPServer
                            description: ManifestSource is the source for manifest
                              files (HTTPServer or LocalPath).
                            type: string
                        type: object
                      config:
                        description: Config holds CNI-specific configuration.
                        properties:
                          calico:
                            description: Calico holds Calico-specific configuration.
                            properties:
                              bgp:
                                description: BGP holds BGP peering configuration.
                                properties:
                                  enabled:
                                    description: Enabled enables BGP peering.
                                    type: boolean
                                  peerIPs:
                                    description: PeerIPs is the list of BGP peer IPs.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              ipam:
                                default: CalicoIPAM
                                description: IPAM is the IP address management mode
                                  (CalicoIPAM or HostLocal).
                                type: string
                              mtu:
                                description: MTU is the network MTU (0 = auto-detect).
                                type: integer
                              typha:
                                description: Typha holds Typha deployment configuration.
                                properties:
                                  enabled:
                                    description: Enabled enables Typha deployment.
                                    type: boolean
                                  replicas:
                                    description: Replicas is the number of Typha replicas.
                                    type: integer
                                type: object
                            type: object
                          cilium:
                            description: Cilium holds Cilium-specific configuration.
                            properties:
                              hubble:
                                description: Hubble holds Hubble observability configuration.
                                properties:
                                  enabled:
                                    description: Enabled enables Hubble observability.
                                    type: boolean
                                  relay:
                                    description: Relay enables Hubble Relay.
                                    type: boolean
                                  ui:
                                    description: UI enables Hubble UI.
                                    type: boolean
                                type: object
                              ipv4NativeRoutingCIDR:
                                description: IPv4NativeRoutingCIDR is the native routing
                                  CIDR.
                                type: string
                              kubeProxyReplacement:
                                default: partial
                                description: KubeProxyReplacement enables kube-proxy
                                  replacement.
                                type: string
                              routingMode:
                                default: tunnel
                                description: RoutingMode is the routing mode (native
                                  or tunnel).
                                type: string
                            type: object
                          flannel:
                            description: Flannel holds Flannel-specific configuration.
                            properties:
                              backend:
                                default: vxlan
                                description: Backend is the backend type (vxlan, host-gw,
                                  wireguard).
                                type: string
                              mtu:
                                description: MTU is the network MTU (0 = auto-detect).
                                type: integer
                            type: object
                          podCIDR:
                            description: PodCIDR is the Pod network CIDR (auto-detected
                              from Cluster if empty).
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled indicates whether CNI installation is
                          enabled.
                        type: boolean
                      installMode:
                        default: Manifest
                        description: InstallMode defines how the CNI plugin is installed
                          (Manifest or Helm).
                        type: string
                      type:
                        default: calico
                        description: Type is the CNI plugin type (calico, cilium,
                          flannel).
                        type: string
                      upgradeStrategy:
                        default: RollingUpdate
                        description: UpgradeStrategy defines the upgrade strategy
                          for CNI plugin.
                        type: string
                      version:
                        description: Version is the desired CNI plugin version.
                        type: string
                    type: object
                  containerRuntime:
                    description: ContainerRuntime specifies the container runtime
                      configuration.
                    properties:
                      config:
                        description: |-
                          Config holds runtime-specific configuration.
                          Applied during installation/upgrade.
                        properties:
                          maxConcurrentDownloads:
                            description: MaxConcurrentDownloads sets max concurrent
                              downloads.
                            type: integer
                          rawConfig:
                            description: RawConfig is raw TOML configuration appended
                              to the final config.
                            type: string
                          registryMirrors:
                            description: RegistryMirrors declares registry mirror
                              configuration.
                            items:
                              description: RegistryMirrorEntry declares a registry
                                mirror configuration.
                              properties:
                                endpoints:
                                  description: Endpoints is the list of mirror endpoints.
                                  items:
                                    type: string
                                  type: array
                                host:
                                  description: Host is the registry host to mirror.
                                  type: string
                              required:
                              - endpoints
                              - host
                              type: object
                            type: array
                          sandboxImage:
                            description: SandboxImage is the pause/sandbox image.
                            type: string
                          systemdCgroup:
                            description: SystemdCgroup enables systemd cgroup driver.
                            type: boolean
                        type: object
                      registryMirrors:
                        description: RegistryMirrors is a list of registry mirror
                          URLs (legacy, use Config instead).
                        items:
                          type: string
                        type: array
                      type:
                        default: containerd
                        description: Type is the container runtime type (containerd,
                          cri-o, docker).
                        type: string
                      version:
                        description: Version is the desired version of the container
                          runtime.
                        type: string
                    type: object
                  csi:
                    description: CSI defines the CSI driver installation configuration.
                    properties:
                      airGap:
                        description: AirGap defines air-gapped installation configuration
                          for CSI.
                        properties:
                          chartArchive:
                            description: ChartArchive is the path to the Helm chart
                              archive.
                            type: string
                          enabled:
                            description: Enabled indicates whether air-gapped installation
                              is used.
                            type: boolean
                          httpServerConfig:
                            description: HTTPServerConfig is the HTTP server configuration.
                            properties:
                              basePath:
                                description: BasePath is the base path for serving
                                  content.
                                type: string
                              baseUrl:
                                description: BaseURL is the HTTP server URL serving
                                  binary packages.
                                type: string
                              enabled:
                                default: false
                                description: Enabled enables the HTTP server.
                                type: boolean
                              insecureSkipVerify:
                                description: InsecureSkipVerify skips TLS verification
                                  (for internal CAs).
                                type: boolean
                              port:
                                default: 8080
                                description: Port is the HTTP server port.
                                type: integer
                              tlsSecretRef:
                                description: TLSSecretRef references a Secret containing
                                  TLS client certificate.
                                type: string
                            type: object
                          localPath:
                            description: LocalPath is the local path for manifest
                              files.
                            type: string
                          manifestSource:
                            description: ManifestSource is the source for manifest
                              files.
                            type: string
                        type: object
                      config:
                        description: Config holds CSI driver specific configuration.
                        properties:
                          cephCsi:
                            description: CephCsi holds Ceph-CSI configuration.
                            properties:
                              cephfs:
                                description: CephFS holds CephFS configuration.
                                properties:
                                  enabled:
                                    description: Enabled enables CephFS support.
                                    type: boolean
                                  fuseMount:
                                    description: FuseMount enables FUSE mount.
                                    type: boolean
                                  kernelMount:
                                    description: KernelMount enables kernel mount.
                                    type: boolean
                                type: object
                              clusterID:
                                description: ClusterID is the Ceph cluster ID.
                                type: string
                              monitors:
                                description: Monitors is the list of Ceph monitor
                                  endpoints.
                                items:
                                  type: string
                                type: array
                              rbd:
                                description: RBD holds RBD configuration.
                                properties:
                                  enabled:
                                    description: Enabled enables RBD support.
                                    type: boolean
                                  pool:
                                    description: Pool is the Ceph RBD pool name.
                                    type: string
                                type: object
                              storageClass:
                                description: StorageClass defines the StorageClass
                                  to create.
                                properties:
                                  fsType:
                                    description: FSType is the filesystem type.
                                    type: string
                                  mountOptions:
                                    description: MountOptions is the list of mount
                                      options.
                                    items:
                                      type: string
                                    type: array
                                  name:
                                    description: Name is the StorageClass name.
                                    type: string
                                  parameters:
                                    additionalProperties:
                                      type: string
                                    description: Parameters is the storage class parameters.
                                    type: object
                                  reclaimPolicy:
                                    default: Delete
                                    description: ReclaimPolicy is the reclaim policy
                                      (Delete or Retain).
                                    type: string
                                  volumeBindingMode:
                                    description: VolumeBindingMode is the volume binding
                                      mode.
                                    type: string
                                required:
                                - name
                                type: object
                            required:
                            - clusterID
                            - monitors
                            type: object
                          cinderCsi:
                            description: CinderCsi holds Cinder-CSI configuration.
                            properties:
                              openstackCloudConfigSecret:
                                description: OpenstackCloudConfigSecret references
                                  the Secret containing cloud-config.
                                type: string
                              storageClass:
                                description: StorageClass defines the StorageClass
                                  to create.
                                properties:
                                  fsType:
                                    description: FSType is the filesystem type.
                                    type: string
                                  mountOptions:
                                    description: MountOptions is the list of mount
                                      options.
                                    items:
                                      type: string
                                    type: array
                                  name:
                                    description: Name is the StorageClass name.
                                    type: string
                                  parameters:
                                    additionalProperties:
                                      type: string
                                    description: Parameters is the storage class parameters.
                                    type: object
                                  reclaimPolicy:
                                    default: Delete
                                    description: ReclaimPolicy is the reclaim policy
                                      (Delete or Retain).
                                    type: string
                                  volumeBindingMode:
                                    description: VolumeBindingMode is the volume binding
                                      mode.
                                    type: string
                                required:
                                - name
                                type: object
                            required:
                            - openstackCloudConfigSecret
                            type: object
                          localCsi:
                            description: LocalCsi holds Local-CSI configuration.
                            properties:
                              storageClass:
                                description: StorageClass defines the StorageClass
                                  to create.
                                properties:
                                  fsType:
                                    description: FSType is the filesystem type.
                                    type: string
                                  mountOptions:
                                    description: MountOptions is the list of mount
                                      options.
                                    items:
                                      type: string
                                    type: array
                                  name:
                                    description: Name is the StorageClass name.
                                    type: string
                                  parameters:
                                    additionalProperties:
                                      type: string
                                    description: Parameters is the storage class parameters.
                                    type: object
                                  reclaimPolicy:
                                    default: Delete
                                    description: ReclaimPolicy is the reclaim policy
                                      (Delete or Retain).
                                    type: string
                                  volumeBindingMode:
                                    description: VolumeBindingMode is the volume binding
                                      mode.
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                          nfsCsi:
                            description: NfsCsi holds NFS-CSI configuration.
                            properties:
                              server:
                                description: Server is the NFS server address.
                                type: string
                              share:
                                description: Share is the NFS export path.
                                type: string
                              storageClass:
                                description: StorageClass defines the StorageClass
                                  to create.
                                properties:
                                  fsType:
                                    description: FSType is the filesystem type.
                                    type: string
                                  mountOptions:
                                    description: MountOptions is the list of mount
                                      options.
                                    items:
                                      type: string
                                    type: array
                                  name:
                                    description: Name is the StorageClass name.
                                    type: string
                                  parameters:
                                    additionalProperties:
                                      type: string
                                    description: Parameters is the storage class parameters.
                                    type: object
                                  reclaimPolicy:
                                    default: Delete
                                    description: ReclaimPolicy is the reclaim policy
                                      (Delete or Retain).
                                    type: string
                                  volumeBindingMode:
                                    description: VolumeBindingMode is the volume binding
                                      mode.
                                    type: string
                                required:
                                - name
                                type: object
                            required:
                            - server
                            - share
                            type: object
                        type: object
                      driver:
                        description: Driver is the CSI driver type (ceph-csi, cinder-csi,
                          local-csi, nfs-csi).
                        type: string
                      enabled:
                        description: Enabled indicates whether CSI installation is
                          enabled.
                        type: boolean
                      installMode:
                        default: Helm
                        description: InstallMode defines how the CSI driver is installed
                          (Manifest or Helm).
                        type: string
                      version:
                        description: Version is the desired CSI driver version.
                        type: string
                    type: object
                  enabled:
                    default: true
                    description: Enabled indicates whether automatic component installation
                      is enabled.
                    type: boolean
                  kubernetes:
                    description: Kubernetes specifies the Kubernetes components configuration.
                    properties:
                      config:
                        description: |-
                          Config holds kubernetes component configuration.
                          Applied during installation/upgrade.
                        properties:
                          kubelet:
                            description: Kubelet holds kubelet-specific configuration.
                            properties:
                              cgroupDriver:
                                default: systemd
                                description: CgroupDriver sets the cgroup driver (cgroupfs
                                  or systemd).
                                type: string
                              extraArgs:
                                additionalProperties:
                                  type: string
                                description: ExtraArgs declares additional kubelet
                                  command-line arguments.
                                type: object
                              maxPods:
                                description: MaxPods sets the maximum number of pods.
                                type: integer
                              rawConfig:
                                description: RawConfig is raw kubelet configuration
                                  (YAML format).
                                type: string
                            type: object
                        type: object
                      repository:
                        description: Repository is the custom package repository configuration.
                        properties:
                          baseUrl:
                            description: BaseURL is the base URL of the package repository.
                            type: string
                          gpgKey:
                            description: GPGKey is the URL to the GPG key for the
                              repository.
                            type: string
                        type: object
                      version:
                        description: Version is the desired Kubernetes version.
                        type: string
                    type: object
                  maxRetries:
                    default: 3
                    description: MaxRetries is the maximum number of retries for installation.
                    type: integer
                  rollbackOnError:
                    description: RollbackOnError indicates whether to rollback on
                      installation failure.
                    type: boolean
                  strategy:
                    default: InstallIfMissing
                    description: Strategy defines the installation strategy.
                    type: string
                  timeout:
                    description: Timeout is the maximum time to wait for installation
                      to complete.
                    type: string
                type: object
              credentialsRef:
                description: |-
                  CredentialsRef references the Secret containing SSH credentials.
                  This can be specified directly or allocated from HostInventory.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              firewall:
                description: Firewall holds configuration for firewall management.
                properties:
                  additionalPorts:
                    description: AdditionalPorts is a list of additional ports to
                      open.
                    items:
                      description: PortRule defines a port to open in the firewall.
                      properties:
                        description:
                          description: Description is a human-readable description
                            of the port.
                          type: string
                        port:
                          description: Port is the port number.
                          type: integer
                        protocol:
                          default: tcp
                          description: Protocol is the protocol (tcp/udp).
                          type: string
                      required:
                      - port
                      type: object
                    type: array
                  autoDetect:
                    default: true
                    description: AutoDetect indicates whether to auto-detect the firewall
                      manager.
                    type: boolean
                  configure:
                    default: true
                    description: Configure indicates whether to automatically configure
                      firewall rules.
                    type: boolean
                type: object
              hostInventoryRef:
                description: HostInventoryRef references the BareMetalHostInventory
                  to allocate a host from.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              hostName:
                description: |-
                  HostName is the hostname of the bare metal machine.
                  This can be specified directly or allocated from HostInventory.
                type: string
              ipAddress:
                description: |-
                  IPAddress is the IP address of the bare metal machine.
                  This can be specified directly or allocated from HostInventory.
                type: string
              nodeBootstrap:
                description: |-
                  NodeBootstrap holds configuration for node bootstrapping.
                  When enabled, CAPBM will automatically configure the node
                  after pre-flight checks pass.
                properties:
                  disableSwap:
                    default: true
                    description: DisableSwap indicates whether to disable swap.
                    type: boolean
                  enabled:
                    default: true
                    description: Enabled indicates whether node bootstrapping is enabled.
                    type: boolean
                  hostname:
                    description: |-
                      Hostname is the desired hostname for the node.
                      If empty, uses the hostName from spec.
                    type: string
                  hostsEntries:
                    description: HostsEntries is a list of additional /etc/hosts entries.
                    items:
                      description: HostsEntry defines a /etc/hosts entry.
                      properties:
                        hostnames:
                          description: Hostnames is the list of hostnames for this
                            IP.
                          items:
                            type: string
                          type: array
                        ip:
                          description: IP is the IP address.
                          type: string
                      required:
                      - hostnames
                      - ip
                      type: object
                    type: array
                  kernelModules:
                    description: KernelModules is a list of kernel modules to load.
                    items:
                      type: string
                    type: array
                  sysctlParams:
                    additionalProperties:
                      type: string
                    description: SysctlParams is a list of sysctl parameters to configure.
                    type: object
                  timeSync:
                    default: true
                    description: TimeSync indicates whether to configure time synchronization.
                    type: boolean
                type: object
              powerManagement:
                description: PowerManagement holds optional power management configuration.
                properties:
                  address:
                    description: Address is the BMC address.
                    type: string
                  credentialsRef:
                    description: CredentialsRef references the Secret containing BMC
                      credentials.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  type:
                    description: Type is the power management protocol type (ipmi,
                      redfish).
                    type: string
                required:
                - address
                - credentialsRef
                - type
                type: object
              providerID:
                description: ProviderID will be the machine name in ProviderID format
                  (baremetal://<hostname>).
                type: string
              releaseImageRef:
                description: |-
                  ReleaseImageRef references the ReleaseImage for component versions.
                  When set, component versions are sourced from the ReleaseImage.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              role:
                description: |-
                  Role indicates the role of this machine (control-plane or worker).
                  Used to filter hosts from the inventory.
                type: string
              selinux:
                description: SELinux holds configuration for SELinux management.
                properties:
                  configure:
                    default: true
                    description: Configure indicates whether to automatically configure
                      SELinux.
                    type: boolean
                type: object
              sshPort:
                default: 22
                description: SSHPort is the SSH port to connect to.
                type: integer
              useSudo:
                description: |-
                  UseSudo indicates whether to use sudo for privileged operations.
                  When true, all installation scripts will be executed with sudo.
                type: boolean
            type: object
          status:
            description: BareMetalMachineStatus defines the observed state of BareMetalMachine.
            properties:
              addresses:
                description: Addresses contains the addresses associated with this
                  machine.
                items:
                  description: MachineAddress contains information for the node's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: type is the machine address type, one of Hostname,
                        ExternalIP, InternalIP, ExternalDNS or InternalDNS.
                      enum:
                      - Hostname
                      - ExternalIP
                      - InternalIP
                      - ExternalDNS
                      - InternalDNS
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions defines current service state of the BareMetalMachine.
                items:
                  description: |-
                    Condition defines an observation of a Cluster API resource operational state.

                    Deprecated: This type is deprecated and is going to be removed when support for v1beta1 will be dropped. Please see https://github.com/kubernetes-sigs/cluster-api/blob/main/docs/proposals/20240916-improve-status-in-CAPI-resources.md for more details.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              installedComponents:
                description: InstalledComponents tracks the versions of installed
                  components.
                properties:
                  cni:
                    description: CNI is the installed CNI plugin version.
                    type: string
                  cniType:
                    description: CNIType is the installed CNI plugin type.
                    type: string
                  containerRuntime:
                    description: ContainerRuntime is the installed container runtime
                      version.
                    type: string
                  csi:
                    description: CSI is the installed CSI driver version.
                    type: string
                  csiDriver:
                    description: CSIDriver is the installed CSI driver type.
                    type: string
                  kubeadm:
                    description: Kubeadm is the installed kubeadm version.
                    type: string
                  kubectl:
                    description: Kubectl is the installed kubectl version.
                    type: string
                  kubelet:
                    description: Kubelet is the installed kubelet version.
                    type: string
                  osType:
                    description: OSType is the detected OS type.
                    type: string
                  osVersion:
                    description: OSVersion is the detected OS version.
                    type: string
                type: object
              providerID:
                description: ProviderID is the unique identifier for this machine.
                type: string
              ready:
                description: Ready indicates that the machine is ready.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
