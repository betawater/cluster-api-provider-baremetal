---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
    cluster.x-k8s.io/v1beta2: v1beta2
  name: baremetalhostinventories.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: BareMetalHostInventory
    listKind: BareMetalHostInventoryList
    plural: baremetalhostinventories
    singular: baremetalhostinventory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Total hosts
      jsonPath: .status.totalHosts
      name: Total
      type: integer
    - description: Available hosts
      jsonPath: .status.availableHosts
      name: Available
      type: integer
    - description: Allocated hosts
      jsonPath: .status.allocatedHosts
      name: Allocated
      type: integer
    - description: Time duration since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: BareMetalHostInventory is the Schema for the baremetalhostinventories
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BareMetalHostInventorySpec defines the desired state of BareMetalHostInventory.
            properties:
              hosts:
                description: Hosts is the list of bare metal hosts in this inventory.
                items:
                  description: HostEntry defines a single bare metal host in the inventory.
                  properties:
                    credentialsRef:
                      description: CredentialsRef references the Secret containing
                        SSH credentials.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    hostName:
                      description: HostName is the hostname of the bare metal machine.
                      type: string
                    ipAddress:
                      description: IPAddress is the IP address of the bare metal machine.
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels are user-defined labels for the host.
                      type: object
                    name:
                      description: Name is the unique identifier for this host entry.
                      type: string
                    role:
                      description: Role indicates the role of this host (control-plane
                        or worker).
                      type: string
                    sshPort:
                      default: 22
                      description: SSHPort is the SSH port to connect to.
                      type: integer
                  required:
                  - credentialsRef
                  - hostName
                  - ipAddress
                  - name
                  type: object
                type: array
            required:
            - hosts
            type: object
          status:
            description: BareMetalHostInventoryStatus defines the observed state of
              BareMetalHostInventory.
            properties:
              allocatedHosts:
                description: AllocatedHosts is the number of allocated hosts.
                type: integer
              availableHosts:
                description: AvailableHosts is the number of available hosts.
                type: integer
              hostsStatus:
                description: HostsStatus contains the status of each host.
                items:
                  description: HostStatusEntry defines the status of a single host.
                  properties:
                    clusterRef:
                      description: ClusterRef references the cluster that has allocated
                        this host.
                      properties:
                        apiVersion:
                          description: API version of the referent.
                          type: string
                        fieldPath:
                          description: |-
                            If referring to a piece of an object instead of an entire object, this string
                            should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                            For example, if the object reference is to a container within a pod, this would take on a value like:
                            "spec.containers{name}" (where "name" refers to the name of the container that triggered
                            the event) or if no container name is specified "spec.containers[2]" (container with
                            index 2 in this pod). This syntax is chosen only to have some well-defined way of
                            referencing a part of an object.
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                          type: string
                        resourceVersion:
                          description: |-
                            Specific resourceVersion to which this reference is made, if any.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          type: string
                        uid:
                          description: |-
                            UID of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    name:
                      description: Name is the name of the host.
                      type: string
                    state:
                      description: State is the current state of the host.
                      type: string
                  required:
                  - name
                  - state
                  type: object
                type: array
              totalHosts:
                description: TotalHosts is the total number of hosts in the inventory.
                type: integer
            required:
            - allocatedHosts
            - availableHosts
            - totalHosts
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
